Los archivos que se suban al servidor poseen la siguiente estructuración:

----Uploads = carpteta principal donde se suber archivos
	-----0000000 = carpeta con el # de afiliado
		-----000000 = carpera con el # de tiquete con los 				   archivos